[project]
name = "wisper-flow-clone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Divaxshah", email = "divax.shah@avinyaaedtech.com" }
]
requires-python = ">=3.10,<3.14"
dependencies = [
    "gradio>=6.26.0",
    "librosa>=0.11.0",
    "soundfile>=0.14.0",
    "torch>=2.6",
]

[project.scripts]
wisper-flow-clone = "wisper_flow_clone.app:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
